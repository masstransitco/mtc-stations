name = "pmtiles-cors-proxy"
main = "cloudflare-worker-pmtiles-cors.js"
compatibility_date = "2024-01-01"
workers_dev = true

# Route for custom domain (when DNS is configured)
# routes = [
#   { pattern = "tiles.air.zone/*", zone_name = "air.zone" }
# ]

[[r2_buckets]]
binding = "PMTILES_BUCKET"
bucket_name = "mtc-buildings-pmtiles"
